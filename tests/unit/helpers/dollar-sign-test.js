
import { dollarSign } from 'e-commerce/helpers/dollar-sign';
import { module, test } from 'qunit';

module('Unit | Helper | dollar sign');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dollarSign([42]);
  assert.ok(result);
});

