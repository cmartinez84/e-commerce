
import { averageRating } from 'e-commerce/helpers/average-rating';
import { module, test } from 'qunit';

module('Unit | Helper | average rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = averageRating([42]);
  assert.ok(result);
});

