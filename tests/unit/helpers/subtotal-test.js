
import { subtotal } from 'e-commerce/helpers/subtotal';
import { module, test } from 'qunit';

module('Unit | Helper | subtotal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subtotal([42]);
  assert.ok(result);
});

